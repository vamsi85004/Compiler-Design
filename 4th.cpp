#include <stdio.h>

int main() {
    // Define variables
    int s, a, b, c;

    // Assign values to variables (for example)
    a = 10;
    b = 3;
    c = 2;

    // Compute the expression
    s = a - b * c;

    // Print the result
    printf("s = %d\n", s);

    return 0;
}


#include <stdio.h>

int main() {
    // Define variables
    int P, a, b, c;

    // Assign values to variables (for example)
    a = 3;
    b = 4;
    c = 5;

    // Compute the expression
    P = (a * b) + (b * c) * 2;

    // Print the result
    printf("P = %d\n", P);

    return 0;
}

